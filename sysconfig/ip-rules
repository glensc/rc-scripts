# ip rules
#
# rules: to, from, iif, tos, fwmark, dev, pref
# rule arguments: prefix, firewall mark, TOS number, device, preference value
# destinations:	table, nat, special route (prohibit | reject | unreachable),
# realms		
# destination arguments: table IP/name, address, realms
#
# Entry format:
<rule>	<rule-arg>	<dest>	<dest-arg>

# examples:
#from 10.0.1/24 	table	100
# you can specify multiple rules and/or destinations:
#from 10.0.1/24 to 10.2/16 fwmark 3	table 4 nat 10.3.0.1
